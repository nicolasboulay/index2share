(*Version number, to be updated by a script, bug correction*)
let version = 53

(*manual version*)
(* minor number :backward compatible feature added*)
let release = 4

(* major number : not-backward compatible feature added (incompatible API)*)
let major = 0
